name: Fix Slinky Storage Compatibility

on:
  workflow_dispatch:

jobs:
  manual_only:
    runs-on: ubuntu-latest
    steps:
      - name: Manual placeholder
        run: echo "This workflow is intentionally manual-only."
